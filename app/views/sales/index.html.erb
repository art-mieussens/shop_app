<h1>Ventas</h1>

<%= form_tag sales_path, :method => :get do %>
	<p>
		<%= label_tag 'Fecha inicial:' %> <%= select_date params_to_date(params[:start_date]) || Date.today, :order => [ :day, :month, :year ], :prefix => :start_date %> <br />
		<%= label_tag 'Fecha final:' %> <%= select_date params_to_date(params[:end_date]) || Date.today, :order => [ :day, :month, :year ], :prefix => :end_date %> <br />			
		<%= submit_tag "Filtrar", :name => nil %> <br />
	</p>
<% end %>

Número de ventas: <%= @sales_number %> <br />
Monto total: <%= to_euros @sales_total %> <br />
<br />
<%= paginate @sales %>
<table>
	<th>id</th><th>Ticket #</th> <th>F.cierre</th> <th>H.cierre</th> <th>Monto</th>
	<%= render @sales %>
</table>

<%= form_tag :url => sales_path, :method => :post %>
